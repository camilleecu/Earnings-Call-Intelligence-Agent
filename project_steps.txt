# 1. possible source
from ast import For


| Dataset      | Focus                          | Why It Matters                                                                                    |
| ------------ | ------------------------------ | ------------------------------------------------------------------------------------------------- |
| GAIA         | Tool-augmented assistant tasks | Tiered difficulty with hidden test answers; strong general assistant benchmark opendatascience    |
| WebArena     | Web-task execution             | Execution-validated in self-hosted sites; ideal for reproducible regression tests opendatascience |
| SWE-bench    | Coding agents                  | Real GitHub issues → patches, scored by containerized tests opendatascience                       |
| BFCL dataset | Function-calling accuracy      | Executable eval using prompts + tool docs for tool-routing accuracy opendatascience               |
| AgentBench   | Multi-environment agents       | Eight environments (OS, DB, web) for scoring LLM-as-agent behavior opendatascience                |



filingapi.dev / earningscalls.dev


One speaker segment = one vector chunk

Each segment should carry:

speaker_type: executive, analyst, operator, attendee

speaker_name: Named individual (~93% coverage)

speaker_role: CEO, CFO, VP of Product, etc.

segment_type: prepared_remarks or Q&A


# Suggested Project Timeline
| Week | Task                                                                         |
| ---- | ---------------------------------------------------------------------------- |
| 1    | Set up docker-compose, pgvector schema, Kestra flow, ingest 5–10 transcripts |
| 2    | Build retrieval pipeline (hybrid search, re-ranking, query rewriting)        |
| 3    | Build LLM flow + evaluation scripts for retrieval and generation             |
| 4    | Build Streamlit UI + feedback collection + Grafana dashboard                 |
| 5    | Documentation, polish, peer review                                           |


# Project Architecture: Earnings Call Intelligence Agent

| Criterion        | Your  Choice                 | Rationale  |
| Ingestion        | Kestra + Python              | Automated pipeline with scheduled runs |
| Knowledge base   | PostgreSQL + pgvector        | Hybrid search (text + vector) out of the box |
| LLM              |Gemini |    |
| Interface        | Streamlit                    | Fastest path to a 2-point UI with built-in feedback  widget |
| Monitoring       | Grafana + SQLite/PostgreSQL  | 5+ charts on query volume, latency, retrieval scores, feedback ratings         |
| Containerization | docker-compose               | Everything — app, db, Kestra, Grafana — in one compose file for 2 points       |



# Pipeline Architecture

- Ingest: Poll the API with cursor-based pagination for incremental sync

- Filter: Skip operator segments; optionally split overly long executive answers

- Embed: Use a financial-domain embedding model (e.g., Arctic-Embed 2.0, BGE-large-en) or a general model with strong long-context performance

- Index: Store in pgvector with metadata-filtering support on ticker, sector, date, and speaker_type

- Retrieve: Pre-filter by metadata, then semantic search over the subset


1. ingestion.py contains reusable functions like fetch_data(), transform(), load_to_db().

2. Kestra calls ingestion.py inside a task.

3. Kestra schedules the workflow daily or whenever new data arrives.

4. Your app reads from the database or vector index that the workflow maintains




# Index
text search + vector search -> hybrid search

text search: 

vector search:

Hybrid search:
. Hybrid search
Both vector and text search have their strengths and weaknesses. Vector search matches by meaning, so it finds relevant pages even when they use words different from the query. But it can miss exact terms like names, codes, or rare keywords. Text search is the opposite: it nails exact words but misses paraphrases and synonyms.

We don't have to pick one or the other - we can use both and merge their results. This approach is called "hybrid search".

Each search produces its own ranked list, so we need a way to combine them into one. In this homework we use Reciprocal Rank Fusion (RRF). It ignores the raw scores from each method, which live on different scales and aren't directly comparable. Instead, it looks only at the position of each document in each list.

Every document scores by its position (rank, starting at 0) in each list, and we sum the scores across lists with a constant k = 60:

RRF(d) = sum over lists of 1 / (k + rank(d))

"Sum over lists" means we go through every ranked list and, for each list where the document appears, add its 1 / (k + rank) contribution. A document found by both searches collects a score from each list, while one found by only a single search collects just one.

The constant k controls how much the exact rank matters. A larger k flattens the gap between positions, so the difference between rank 0 and rank 5 counts for less. A smaller k does the opposite: it sharpens that gap, so being at the top of a list matters much more.

The value 60 comes from the original RRF paper and is the usual default. You rarely need to tune it. Lower it when only the top results matter. Raise it to reward documents that appear across many lists, even when they never quite reach the top.

def rrf(result_lists, k=60, num_results=5):
    scores = {}
    docs = {}

    for results in result_lists:
        for rank, doc in enumerate(results):
            key = (doc["filename"], doc.get("start", 0))
            scores[key] = scores.get(key, 0) + 1 / (k + rank)
            docs[key] = doc

    ranked = sorted(scores, key=scores.get, reverse=True)
    return [docs[key] for key in ranked[:num_results]]


# Retrieval Flow (2 points)
User Query → Query Rewriting → Hybrid Search (pgvector) → Re-ranking → Prompt Building → LLM → Response


# Retrieval Evaluation (2 points)


# LLM Evaluation (2 points)



# Containerization
| Container       | Responsibility                                                           |
| --------------- | ------------------------------------------------------------------------ |
| app             | Streamlit earnings-call RAG interface                                    |
| ingestion       | Runs scheduled_ingestion.py on demand or under Kestra                    |
| postgres        | Stores transcript chunks, embeddings, conversations, and monitoring data |
| kestra          | Orchestrates the scheduled ingestion workflow                            |
| kestra_postgres | Stores Kestra’s own orchestration metadata                               |


What “others can run” should mean
Rather than publishing a single prebuilt image immediately, start with:

a Dockerfile for the application,

a Dockerfile.ingestion for scheduled ingestion,

a docker-compose.yml that builds and connects all services,

.env.example with variable names only,

clear README commands.

Then anyone can run:

bash
git clone <your-repository>
cd llm_project2026
cp .env.example .env
# Add their own ROIC and Gemini keys
docker compose up --build
That is generally better for a course project than asking people to pull a private/local image.


# Container architecture
Browser
   │
   ▼
Streamlit app container
   │
   ├── Gemini API
   │
   ▼
Postgres + pgvector container
   ▲
   │
Ingestion container ──► ROIC API
   ▲
   │
Kestra container ──────► triggers ingestion on schedule